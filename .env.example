# Copy to .env for local runs (loaded manually, e.g. `export $(cat .env | xargs)`).
# In GitHub Actions this is provided via the DISCORD_WEBHOOK_URL repository secret.
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxxxxxxx/yyyyyyyy
